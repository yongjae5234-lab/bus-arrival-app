services:
  - type: web
    name: bus-arrival-app
    runtime: python
    plan: free  # <--- 이 줄을 반드시 추가해야 무료로 인식합니다!
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
